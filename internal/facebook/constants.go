package facebook

// Facebook API form field parameter keys.
const (
	paramAccessToken = "access_token"
	paramMessage     = "message"
	paramVideoID     = "video_id"
)

// Facebook API form field key for image URL.
const paramURL = "url"

// Upload phase identifiers for the Reels/Stories two-phase upload API.
const (
	uploadPhaseStart  = "start"
	uploadPhaseFinish = "finish"
)
